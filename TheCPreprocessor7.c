// Use two(2) statement after #define . if we want to do this then we need to use (Backslash) \
// Created by KUNTAL OJHA RAJ on 8/4/2020.
// very interesting to use two statement
#include<stdio.h>
#define HL for (i=0;i<=79;i++)\
printf("%c",196);
// #define HL for (i=0;i<=79;i++)\ we, need to tell what is i Then it work otherwise it can't work.
//printf("%c",196);
int main()
{
    int i;
    // It print ────────────────────────────────────────────────────────────────────────────────
    HL // Or HL;
}